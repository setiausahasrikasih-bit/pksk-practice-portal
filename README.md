# PKSK Practice Portal

Static PKSK practice portal connected to Supabase.

## Deploy

Import this repository into Vercel as a static project. No build command is required.

- Framework preset: Other
- Build command: leave blank
- Output directory: leave blank
- Install command: leave blank

The app uses hash routing and `vercel.json` rewrites to `index.html`.

Git deployment connected.
